#' @export
print.hierplane_dataframe <- function(x, ...) {
  cat("<hierplane_tree object: from hp_dataframe>", ...)
}

#' @export
print.hierplane_spacyr <- function(x, ...) {
  cat("<hierplane_tree object: from hp_spacyr>", ...)
}
